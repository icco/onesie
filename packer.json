{
  "variables": {
    "account_file": "",
    "tag": "local"
  },
  "builders": [{
    "type": "googlecompute",
    "account_file": "{{user `account_file`}}",
    "project_id": "icco-cloud",
    "source_image": "debian-8-jessie-v20161215",
    "ssh_username": "packer",
    "image_name": "onesie-image-{{timestamp}}",
    "image_family": "onesie",
    "zone": "us-central1-c"
  }],
  "provisioners": [
    {
      "type": "shell",
      "environment_vars": [
        "DEBIAN_FRONTEND=noninteractive"
      ],
      "inline": [
        "sudo apt-get update",
        "sudo apt-get -y upgrade",
        "sudo apt-get install -y ansible build-essential aptitude vim"
      ]
    },
    {
      "type": "ansible-local",
      "playbook_file": "ansible.yml",
      "playbook_dir": "."
    },
    {
      "type": "file",
      "source": "wrkr",
      "destination": "/opt/"
    }
  ]
}

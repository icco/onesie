{
  "variables": {
    "account_file": "",
    "tag": "local"
  },
  "builders": [{
    "type": "googlecompute",
    "account_file": "{{user `account_file`}}",
    "project_id": "icco-cloud",
    "source_image": "debian-8-jessie-v20161129",
    "ssh_username": "packer",
    "image_family": "onesie",
    "tags": "version:{{user `tag`}}",
    "zone": "us-central1-c"
  }],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sudo apt-get update",
        "sudo apt-get install -y ansible build-essential aptitude varnish nginx vim"
      ]
    },
    {
      "type": "ansible-local",
      "playbook_file": "ansible.yml"
    }
  ]
}
